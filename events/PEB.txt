add_namespace = PEB

country_event = {
    id = PEB.1 # PEB declares war on CFD a week after breaking away
    title = PEB.1.t
    desc = PEB.1.d
    picture = x
    is_triggered_only = yes
    fire_only_once = yes 

    option = {
      name = PEB.1.a

      declare_war_on = {
        target = CFD
        type = annex_everything
      }
      country_event = { id = PEB.2 days = 3 }
  }
}

country_event = {
    id = PEB.2
    title = PEB.2.t
    desc = PEB.2.d
    picture = x
    is_triggered_only = yes
    fire_only_once = yes 

    option = {
      name = PEB.2.a
      INT = {transfer_state = 8}

      country_event = { id = PEB.3 }
  }
} 


country_event = {
    id = PEB.3
    title = PEB.3.t
    desc = PEB.3.d
    picture = x
    is_triggered_only = yes
    fire_only_once = yes 

    option = {
      name = PEB.3.a

INT = {
      declare_war_on = {
        target = CFD
        type = annex_everything
      }

      declare_war_on = {
        target = PEB
        type = annex_everything
      }
    }
  } 
}