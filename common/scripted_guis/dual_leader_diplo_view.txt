scripted_gui = {

	dual_leader_politics_view = {
		context_type = selected_country_context
		parent_window_token = selected_country_view_diplomacy
		window_name = "dual_leader_diplomacy_view_container"

		properties = {
			dual_leader_portrait_diplo = {
				image = "[From.GetDualLeaderPortrait]"
			}
		}
		
		visible = {
			OR = {
				tag = ROR tag = STR tag = PAT tag = XIN tag = DRE tag = CRJ tag = JRM tag = COR tag = HIE tag = ERN tag = RMI tag = HNL tag = USR
				AND = {
					tag = HON
					has_country_flag = HON_allow_dual_leaders
				}	
				AND = {
					tag = SAO
					has_government = democratic
				}
				AND = {
					tag = PRU
					has_government = democratic
				}
				AND = {
					tag = VEN
					has_government = fascism
				}
				AND = {
					tag = ACR
					has_country_flag = marques_elected
				}
				tag = ARG
				AND = {
					tag = SAO
					has_country_flag = second_phase_sp
				}
				AND = {
					tag = CFD
					CFD = {
						OR = {
							has_country_flag = CFD_elections_phase_1
							has_country_flag = CFD_cid_adminstration
						}
					}
				}
				AND = {
					tag = PIM
					PIM = {has_country_flag = CFD_failometer_enabled}
				}
			}
		}
		
		effects = {
		}
		
		triggers = {
		}
	}
}

