scripted_gui = {
	line_graph_cfd_scripted_gui = {
		window_name = "line_graph_cfd_gui"
		context_type = decision_category

		visible = {
			has_country_flag = CFD_development
		}

		effects = {
			#add_button_click = {
			#	# Invoke scripted effect to add a random point to the line
			#	# See scripted effect file
			#
			#	cfd_add_cash_to_line = yes
			#}
		}

		# Set up dynamic list to generate one line segment per point
		dynamic_lists = {
			line_segments = {
				array = CFD_graph_offsets
				change_scope = no
				entry_container = line_graph_line_segment
				index = segment_idx
			}
		}

		properties = {
			segment = {
				y = CFD_graph_offsets^segment_idx

				frame = CFD_graph_slopes^segment_idx
			}
		}

		triggers = {
			# Optional. This hides every other x label, to give them some breathing room
			label_visible = {
				set_temp_variable = { si = segment_idx }
				modulo_temp_variable = { si = 2 }
				check_variable = { si = 0 }
			}
		}
	}
}
