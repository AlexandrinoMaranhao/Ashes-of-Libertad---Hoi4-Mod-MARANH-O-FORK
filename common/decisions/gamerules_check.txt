gamerules_check = {
	test_demo_end = {
		available = {
			AND = {
				is_debug = yes
				is_ai = no
			}
		}
		complete_effect = {
			ROOT = {
				set_country_flag = demo_end
			}
		}
		ai_will_do = {
			factor = -10
		}
	}
	portuclave_activation = {
		days_remove = 1
		fire_only_once = yes
		available = {
			has_game_rule = { rule = portoclave_setup option = PORTOCLAVE_ENABLED }
		}
		activation = {
			has_game_rule = { rule = portoclave_setup option = PORTOCLAVE_ENABLED }
		}
		complete_effect = {
			POR = {
				transfer_state = 2
			}
		}
		ai_will_do = {
			factor = 200
		}
	}
	el_salvadorian_bos_activation = {
		days_remove = 1
		fire_only_once = yes
		available = {
			has_game_rule = { rule = el_salvadorian_bos_setup option = ELSAL_BOS_ON }
		}
		activation = {
			has_game_rule = { rule = el_salvadorian_bos_setup option = ELSAL_BOS_ON }
		}
		complete_effect = {
			ELC = {
				transfer_state = 547
				transfer_state = 541
				#load_oob = "ELC_1967" Have to manually add the OOB after the event.
			}
		}
		ai_will_do = {
			factor = 200
		}
	}
}