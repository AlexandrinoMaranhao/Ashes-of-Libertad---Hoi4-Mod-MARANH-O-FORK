find_resources = {
     develop_infrastructure = {
          complete_effect = {
               random_owned_state = {
                    add_building_construction = { type = infrastructure level = 2 instant_build = yes }
               }
               add_manpower = -500
          }
      }
	  
	  test_this = {
	  visible = { has_variable = acre_test }
	  
	  complete_effect = {
		set_variable = {
		var = overgay
		value = 1
		}
	  }
	  }
	  
	  fuck_you = {
	  visible = { has_variable = overgay }
	  
	  complete_effect = {
		add_political_power = 60
	  }
	  }
}
